import type { Options } from "../../src";

export default {
  spec: {
    Header: [
      {
        action: "note",
        header: "X-Content-Type-Options",
      },
    ],
  },
} as Partial<Options>;
